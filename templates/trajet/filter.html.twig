{{ form_start(form, {attr: {class: 'filter'}}) }}
  {{ form_row(form.q) }}

  <h4>Trajets</h4>
  {{ form_row(form.trajets) }}

    <h4>Prix</h4>
    <div class="row">
      <div class="col-md-8">
        {{ form_row(form.min) }}
      </div>
      <div class="col-md-8">
        {{ form_row(form.max) }}
      </div>
    </div>    

    <button type="submit" class="btn btn-primary w-100">Filtre</button>
{{ form_end(form) }}